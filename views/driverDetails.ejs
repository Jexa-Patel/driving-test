<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Driver Details</title>
    <link rel="stylesheet" href="../public/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>


<body>
    <%- include('layouts/nav'); %>
        <header class="masthead"
            style="background-image:linear-gradient(rgba(47, 23, 15, 0.65),rgba(47, 23, 15, 0.65)), url('assets/img/intro.jpg')">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="site-heading">
                            <h1>DRIVER DETAILS</h1>
                            <span class="subheading">You Can View Candidates Details Here</span>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <div class="container mt-5">
            <h1>Driver Details</h1>
            <p><strong>Name:</strong>
                <%= user.firstname %>
                    <%= user.lastname %>
            </p>
            <p><strong>Car Details:</strong>
                <%= user.car_details.make %>
                    <%= user.car_details.model %> (<%= user.car_details.year %>)
            </p>
            <p><strong>Test Type:</strong>
                <%= user.testType %>
            </p>
            <form action="/examiner/driver/<%= user._id %>" method="POST">
                <div class="form-group">
                    <label for="examinerComment">Comments:</label>
                    <textarea id="examinerComment" name="examinerComment" class="form-control">
                        <%= user.examinerComment %>
                    </textarea>
                </div>
                <div class="form-group">
                    <label for="testResult">Result:</label>
                    <select id="testResult" name="testResult" class="form-control">
                        <option value="pass" <%=user.testResult===true ? "selected" : "" %>>Pass</option>
                        <option value="fail" <%=user.testResult===false ? "selected" : "" %>>Fail</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-success">Submit</button>
            </form>
        </div>
</body>

</html>